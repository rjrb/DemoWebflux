docker run --name postgresql -e POSTGRES_PASSWORD=12345 -p 5431:5432 -d postgres
docker exec postgresql psql -U postgres -c "CREATE DATABASE reactivo;"
docker exec postgresql psql -U postgres -d reactivo -c "CREATE EXTENSION pgcrypto;"
docker exec postgresql psql -U postgres reactivo -c "create table empleados ( codigo uuid default gen_random_uuid() not null constraint empleados_pk primary key, cedula varchar(15) not null, nombre varchar(100) not null, edad integer, ciudad varchar(25) ); comment on table empleados is 'Tabla maestra de empleados'; comment on column empleados.codigo is 'Identificador del empleado'; comment on column empleados.cedula is 'Documento de identidad del empleado'; comment on column empleados.nombre is 'Nombre del empleado'; comment on column empleados.edad is 'Edad del empleado'; comment on column empleados.ciudad is 'Ciudad en que trabaja el empleado'; alter table empleados owner to postgres; create unique index empleados_cedula_uindex on empleados (cedula);"
